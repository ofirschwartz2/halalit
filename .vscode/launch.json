{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Unity Debugger",
            "type": "vstuc",
            "request": "attach",
            "projectPath": "${workspaceFolder}"
        }
    ]
}